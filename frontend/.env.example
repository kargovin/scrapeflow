# Copy to .env.local for local development.
# The publishable key is safe to expose — it's baked into the JS bundle by Vite.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Optional: override the API base URL for local dev (leave blank for same-origin in production)
# VITE_API_BASE_URL=http://localhost:8000
